# 花笺 Floral Notepaper — 修复纪要

> **创建日期**: 2026-06-15  
> **关联文档**: 安全审查报告、修复任务计划、项目索引与任务追踪  
> **Git 基线**: `upstream-v1.1.0`

本文档逐项记录每个已完成修复任务的完整留痕信息，包括：时间、问题描述、根本原因、修复方案、影响分析、改动文件与行号、验证方式。

---

## 目录

- [T1 — 设置 CSP 内容安全策略](#t1--设置-csp-内容安全策略)
- [T2 — Tauri 命令路径遍历防护](#t2--tauri-命令路径遍历防护)
- [T3 — 禁用 Markdown sanitize style 属性](#t3--禁用-markdown-sanitize-style-属性)
- [T4 — 添加 React Error Boundary](#t4--添加-react-error-boundary)
- [T5 — 提交 Cargo.lock](#t5--提交-cargolock)
- [T7 — 统一三处版本号](#t7--统一三处版本号)
- [T8 — 替换 document.execCommand](#t8--替换-documentexeccommand)
- [T9 — Scheduler 线程优雅关闭](#t9--scheduler-线程优雅关闭)
- [T10 — 添加安全扫描 CI](#t10--添加安全扫描-ci)
- [T6 / T11 — 阻塞任务](#t6--t11--阻塞任务)

---

## T1 — 设置 CSP 内容安全策略

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🔴 CRITICAL (P0) |
| **Phase** | Phase 1 — 安全阻断 |
| **状态** | ✅ 已完成 |

### 问题描述

`tauri.conf.json` 中 `"csp": null` 导致 WebView 完全不发送 Content Security Policy 头。结合 `rehype-raw` 允许原始 HTML 渲染，攻击者可通过恶意 `.md` 文件在 WebView 中执行任意 JavaScript，进而调用 Tauri 命令读写用户文件系统。

### 根本原因

Tauri v2 中 `csp: null` 表示显式禁用 CSP。项目在开发过程中未设置 CSP，一直沿用 null 值发布到生产环境。

### 修复方案

将 `null` 替换为严格 CSP 策略：

```
default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; connect-src 'self' https://mirrorchyan.com https://api.github.com
```

策略说明：
- `default-src 'self'` — 所有资源默认仅允许同源
- `script-src 'self'` — 仅允许同源脚本（禁止内联脚本和外部脚本）
- `style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net` — 允许同源样式 + 内联样式（KaTeX 需要）+ KaTeX CDN
- `connect-src 'self' https://mirrorchyan.com https://api.github.com` — 仅允许连接到更新服务器

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **安全性** | 🔒🔒🔒 大幅提升：WebView 无法加载外部脚本，XSS 攻击面从"完整系统访问"缩小为"同源限制" |
| **功能兼容性** | KaTeX CDN 样式已在 CSP 中放行，数学公式渲染不受影响 |
| **副作用** | 如果应用中有其他外部资源加载需求，会被 CSP 阻止。当前检查确认仅 KaTeX CDN 需要 |

### 改动详情

| 文件 | 行号 | 改动前 | 改动后 |
|------|------|--------|--------|
| `src-tauri/tauri.conf.json` | L29 | `"csp": null` | `"csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; connect-src 'self' https://mirrorchyan.com https://api.github.com"` |

### 验证方式

1. 构建后打开开发者工具 → Network 面板 → 确认无 CSP 违规报告
2. 导入包含 `<script>alert(1)</script>` 的恶意 .md 文件 → 脚本不执行
3. KaTeX 数学公式（`$$E=mc^2$$`）正常渲染

### 相关提交

`e90dd00` — Phase 1: 完成 T1(CSP) + T3(style) + 工作目录清理

---

## T2 — Tauri 命令路径遍历防护

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🔴 CRITICAL (P0) |
| **Phase** | Phase 1 — 安全阻断 |
| **状态** | ✅ 已完成 |

### 问题描述

7 个 Tauri 命令接受前端传入的任意路径字符串，直接调用文件系统 API（`read_to_string`、`write`、`copy`、`metadata` 等），无任何路径合法性校验。结合 CSP 被禁用的历史状态，前端 XSS 可获得完整的文件系统读写能力。

### 根本原因

开发时假设文件路径来自文件对话框（可信来源），未考虑前端可能构造恶意路径（如 `../../etc/passwd`）的场景。

### 修复方案

1. **新增 `validate_path()` 辅助函数**，对所有 7 个命令的路径参数进行统一校验：
   - 路径不可为空
   - 路径不可包含空字节（防止 C 风格截断攻击）
   - 路径必须为绝对路径（防止相对路径遍历，如 `../../etc/passwd`）

2. **逐项修改 7 个命令**，将 `PathBuf::from(path)` 替换为 `validate_path(&path)?`

### 受影响的 7 个命令

| 命令 | 行号 | 操作类型 | 风险描述 |
|------|------|----------|----------|
| `notes_import_markdown` | L50-59 | 导入 markdown | 任意文件读取 |
| `notes_export_markdown` | L62-64 | 导出 markdown | 任意位置写入 |
| `read_external_file` | L67-73 | 读取外部文件 | 任意文件读取 |
| `get_file_modified_time` | L76-91 | 获取文件时间 | 元数据泄露 |
| `save_external_file` | L94-107 | 保存外部文件 | 任意位置写入 |
| `images_save_from_path` | L151-161 | 导入图片 | 任意文件复制 |
| `copy_background_image` | L188-223 | 复制背景图 | 任意文件复制 |

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **安全性** | 🔒🔒 大幅提升：相对路径遍历被阻断，空字节注入被防护 |
| **功能兼容性** | 文件对话框返回的路径均为绝对路径，不受影响 |
| **副作用** | 如果前端曾向这些命令传入相对路径，现在会收到 `invalidPath` 错误。当前代码中未发现此场景 |

### 改动详情

| 文件 | 行号范围 | 改动说明 |
|------|----------|----------|
| `src-tauri/src/lib.rs` | L9-44 | 新增 `validate_path()` 函数（37 行） |
| `src-tauri/src/lib.rs` | L88-89 | `notes_import_markdown` → `validate_path` |
| `src-tauri/src/lib.rs` | L97-98 | `notes_export_markdown` → `validate_path` |
| `src-tauri/src/lib.rs` | L103-104 | `read_external_file` → `validate_path` |
| `src-tauri/src/lib.rs` | L113-114 | `get_file_modified_time` → `validate_path` |
| `src-tauri/src/lib.rs` | L132-133,140 | `save_external_file` → `validate_path`（2处） |
| `src-tauri/src/lib.rs` | L191 | `images_save_from_path` → `validate_path` |
| `src-tauri/src/lib.rs` | L227 | `copy_background_image` → `validate_path` |

### 验证方式

1. 正常通过文件对话框导入/导出 → 功能正常
2. 前端尝试调用 `read_external_file("../../etc/passwd")` → 返回 `invalidPath` 错误
3. 前端尝试调用 `read_external_file("C:\Users\...")`（合法绝对路径）→ 正常读取

### 相关提交

`b4288d1` — Phase 1: 完成 T2(路径遍历防护) - 最后一个修复任务

---

## T3 — 禁用 Markdown sanitize style 属性

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🔴 CRITICAL (P0) |
| **Phase** | Phase 1 — 安全阻断 |
| **状态** | ✅ 已完成 |

### 问题描述

`MarkdownPreview.tsx` 中的 `rehype-sanitize` schema 在所有元素 (`"*"`) 上显式允许了 `"style"` 属性。当 `renderHtmlMarkdown` 选项开启时，攻击者可在 markdown 中通过内联 style 实现 CSS 注入，如 `background-image:url(...)` 数据外泄。

### 根本原因

最初可能为了支持 `<span style="color:red">` 等内联样式，在 sanitize schema 中开放了 `style` 属性，但未评估其安全风险。

### 修复方案

从 `"*"` 的属性列表中删除 `"style"`，`rehype-sanitize` 将自动剥离所有元素的 style 属性。

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **安全性** | 🔒 消除 CSS 注入向量。`background-image:url()`, `position:fixed` 等 CSS 攻击无效 |
| **功能兼容性** | Markdown 笔记中通过 HTML `<span style="...">` 设置的内联样式将不再渲染 |

### 改动详情

| 文件 | 行号 | 改动前 | 改动后 |
|------|------|--------|--------|
| `src/features/markdown/MarkdownPreview.tsx` | L78 | `"style",` | （已删除） |

### 验证方式

1. 创建笔记，源码模式输入 `<span style="background-image:url(https://evil.com/steal)">test</span>`，预览模式确认背景图不加载
2. 正常 Markdown 语法（粗体、斜体、标题）渲染正常

### 相关提交

`e90dd00` — Phase 1: 完成 T1(CSP) + T3(style) + 工作目录清理

---

## T4 — 添加 React Error Boundary

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🟠 MAJOR (P1) |
| **Phase** | Phase 2 — 架构强化 |
| **状态** | ✅ 已完成 |

### 问题描述

全量 79 个组件文件中未找到任何 `ErrorBoundary`、`componentDidCatch` 或错误边界包装。任何组件中的未捕获异常都会导致整个 Tauri 应用白屏崩溃。`MarkdownPreview` 组件有大量不安全的类型转换，崩溃风险较高。

### 根本原因

项目在开发阶段未考虑全局错误处理机制，React 的 Error Boundary 是可选功能，容易被忽略。

### 修复方案

1. **新建 `ErrorBoundary` 组件** (`src/components/ErrorBoundary.tsx`)：
   - 使用 React Class Component + `getDerivedStateFromError` + `componentDidCatch`
   - 错误时显示友好回退 UI（🌸 图标 + "应用遇到错误，请重启"）
   - 支持自定义 fallback

2. **修改 `main.tsx`**：用 `<ErrorBoundary>` 包裹 `<App />`

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **用户体验** | 🟢 正面：组件错误不再导致白屏，改为友好回退提示 |
| **开发体验** | `componentDidCatch` 在控制台输出错误详情和组件栈 |
| **副作用** | 无 |

### 改动详情

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/components/ErrorBoundary.tsx` | 🆕 新建 | Error Boundary 类组件（39 行） |
| `src/main.tsx` | L7 | 新增 `import { ErrorBoundary }` |
| `src/main.tsx` | L31-33 | `<App />` 外包裹 `<ErrorBoundary>` |

### 验证方式

1. 在任意渲染组件中抛出异常 → 显示错误回退 UI 而非白屏
2. 控制台输出 `[ErrorBoundary] Uncaught error:` 日志

### 相关提交

`ba29ae6` — Phase 2: 完成 T4(ErrorBoundary) + T5(Cargo.lock)

---

## T5 — 提交 Cargo.lock

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🟠 MAJOR (P1) |
| **Phase** | Phase 2 — 架构强化 |
| **状态** | ✅ 已完成 |

### 问题描述

`Cargo.lock` 未纳入版本控制。CI 构建与本地构建可能解析不同的传递依赖版本，破坏可复现构建和可审计性。

### 根本原因

上游仓库可能将 `Cargo.lock` 加入了 `.gitignore`（常见于库项目），但对于二进制应用（desktop app），根据 Rust 官方建议应当提交。

### 修复方案

经检查，`src-tauri/Cargo.lock` 已存在且未被 `.gitignore` 排除。该文件在初次 git 快照提交 (`6eaac5b`) 中已自动纳入版本控制。

**无需额外操作，此任务在上游快照阶段自动完成。**

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **可复现构建** | ✅ 确保所有构建使用相同的依赖版本 |
| **供应链安全** | ✅ 防止 CI 中意外拉取被篡改的依赖版本 |

### 改动详情

| 文件 | 操作 |
|------|------|
| `src-tauri/Cargo.lock` | 已在初始提交中追踪（400 个文件之一） |

### 相关提交

`6eaac5b` — feat: 花笺 Floral Notepaper v1.1.0 上游源码快照（初次提交）

---

## T7 — 统一三处版本号

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🟡 MINOR (P2) |
| **Phase** | Phase 3 — 体验优化 |
| **状态** | ✅ 已完成 |

### 问题描述

三处版本声明不一致：

| 文件 | 版本 |
|------|------|
| `package.json` | `1.1.0` |
| `src-tauri/Cargo.toml` | `1.0.4` |
| `src-tauri/tauri.conf.json` | `1.1.0` |

`Cargo.toml` 落后于其他两处，可能是上次发布时遗漏了 Rust 端的版本同步。

### 根本原因

项目使用 `scripts/set-version.mjs` 脚本统一同步版本号，但该脚本可能未在上次发布流程中正确执行或 Cargo.toml 被手动修改后未同步。

### 修复方案

将 `Cargo.toml` 的版本从 `1.0.4` 更新为 `1.1.0`，与 `package.json` 和 `tauri.conf.json` 保持一致。

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **编译产物** | 无影响（版本号为声明字段） |
| **用户体验** | 关于页面显示的版本号将一致 |
| **自动化** | 后续版本更新应执行 `node scripts/set-version.mjs <ver>` 统一同步 |

### 改动详情

| 文件 | 行号 | 改动前 | 改动后 |
|------|------|--------|--------|
| `src-tauri/Cargo.toml` | L3 | `version = "1.0.4"` | `version = "1.1.0"` |

### 相关提交

`9cf0991` — Phase 2-3: 完成 T7(版本统一) + T9(Scheduler优雅关闭)

---

## T8 — 替换 document.execCommand

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🟡 MINOR (P2) |
| **Phase** | Phase 3 — 体验优化 |
| **状态** | ✅ 已完成 |

### 问题描述

项目中 4 处使用了已弃用的 `document.execCommand` API，未来浏览器可能彻底移除该 API：

1. `MainWindow.tsx:274` — `execCommand("insertText", ...)` 插入文本
2. `MainWindow.tsx:285` — `execCommand(command)` undo/redo
3. `useImagePaste.ts:53` — `execCommand("insertText", ...)` 插入图片链接
4. `ContextMenu.tsx:164` — `execCommand(command)` contentEditable 回退

### 根本原因

`execCommand` 是历史遗留 API，在现代应用中应迁移到 `navigator.clipboard` 或直接值操作。

### 修复方案

- **第 1 处（MainWindow.tsx:274）**：已有 `setContent(result)` 紧随其后，直接删除 `execCommand` 调用
- **第 2 处（MainWindow.tsx:285）**：`undo`/`redo` 无程序化替代 API，保留并加注释说明
- **第 3 处（useImagePaste.ts:53）**：改为直接操作 `textarea.value` + `dispatchEvent("input")`
- **第 4 处（ContextMenu.tsx:164）**：contentEditable 元素的唯一可用方案，保留并加注释

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **兼容性** | 2 处 insertText 替换后以现代方式工作；2 处 undo/redo/contentEditable 暂留但注释标记 |
| **功能** | 文本插入、剪贴板粘贴、undo/redo 均正常 |
| **副作用** | `useImagePaste.ts` 的改动需要触发 `input` 事件以确保 React 状态同步 |

### 改动详情

| 文件 | 行号 | 改动说明 |
|------|------|----------|
| `src/components/MainWindow.tsx` | L274 | `execCommand("insertText")` → 直接用 `setContent(result)` |
| `src/components/MainWindow.tsx` | L284 | undo/redo 保留，加注释 |
| `src/features/images/useImagePaste.ts` | L52-59 | `execCommand("insertText")` → 直接值操作 + dispatchEvent |
| `src/components/ContextMenu.tsx` | L163 | contentEditable 保留，加注释 |

### 相关提交

`c8d4745` — Phase 3: 完成 T8(execCommand替换) + 标记T6为阻塞

---

## T9 — Scheduler 线程优雅关闭

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🟡 MINOR (P2) |
| **Phase** | Phase 3 — 体验优化 |
| **状态** | ✅ 已完成 |

### 问题描述

`updater/scheduler.rs` 中 `start_auto_check_scheduler` 函数启动一个 `thread::spawn`，内部使用 `loop {}` 无限循环轮询更新检查。应用退出时线程被操作系统强制终止，可能处于 I/O 中间状态，存在极小概率的数据损坏风险。

### 根本原因

原始实现未考虑优雅关闭机制。因为 Tauri 退出时操作系统会强制终止所有子线程，功能上无问题，但不满足正确性要求。

### 修复方案

1. **添加静态关闭标志**：`static SCHEDULER_SHUTDOWN: AtomicBool`，在循环首尾各检查一次
2. **添加 `stop_auto_check_scheduler()` 公开函数**
3. **在 `lib.rs` 的 `RunEvent::Exit` 中调用**：应用退出前设置标志，让线程在下一次循环迭代时自然退出

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **正确性** | ✅ 线程在退出前有机会完成当前 I/O 操作 |
| **性能** | 每个循环额外两次 `Ordering::Relaxed` 原子读，微乎其微 |
| **副作用** | 无 |

### 改动详情

| 文件 | 行号 | 改动说明 |
|------|------|----------|
| `src-tauri/src/updater/scheduler.rs` | L3 | 新增 `use std::sync::atomic::{AtomicBool, Ordering}` |
| `src-tauri/src/updater/scheduler.rs` | L11-24 | 新增 `SCHEDULER_SHUTDOWN` 静态变量、`stop_auto_check_scheduler()`、`reset_scheduler_shutdown()` |
| `src-tauri/src/updater/scheduler.rs` | L27,46-49 | 循环中添加两次 `SCHEDULER_SHUTDOWN` 检查 |
| `src-tauri/src/lib.rs` | L486-488 | 在 `RunEvent::Exit` 分支中调用 `stop_auto_check_scheduler()` |

### 验证方式

1. 修改 `INITIAL_DELAY` 和 `POLL_INTERVAL` 为短时间（测试用），确认循环正常执行
2. 关闭应用，确认线程在下一个 sleep 后退出（可通过日志验证）

### 相关提交

`9cf0991` — Phase 2-3: 完成 T7(版本统一) + T9(Scheduler优雅关闭)

---

## T10 — 添加安全扫描 CI

### 基本信息

| 字段 | 内容 |
|------|------|
| **修复时间** | 2026-06-15 |
| **严重度** | 🟡 MINOR (P2) |
| **Phase** | Phase 4 — 长期建设 |
| **状态** | ✅ 已完成 |

### 问题描述

项目缺少自动化的依赖安全扫描机制，无法及时发现已知漏洞（CVE）。

### 根本原因

项目规模较小，尚未配置安全扫描基础设施。

### 修复方案

创建两个新文件：

1. **`.github/dependabot.yml`** — 配置三种生态系统的自动依赖更新：npm、cargo、github-actions
2. **`.github/workflows/security-scan.yml`** — 包含三个并行任务：
   - `cargo-audit`：Rust 依赖漏洞扫描
   - `npm-audit`：npm 依赖漏洞扫描（`--audit-level=high`）
   - `osv-scanner`：Google 的多语言 OSV 数据库扫描

触发条件：push 到 main、PR 到 main、每周一自动运行、手动触发。

### 影响分析

| 影响维度 | 分析 |
|----------|------|
| **安全性** | 🔒 自动化发现已知漏洞，不需要人工关注 |
| **CI 资源** | 每次安全检查 3 个并行 job，总耗时约 3-5 分钟 |

### 改动详情

| 文件 | 操作 |
|------|------|
| `.github/dependabot.yml` | 🆕 新建（35 行） |
| `.github/workflows/security-scan.yml` | 🆕 新建（54 行） |

### 相关提交

`2d44317` — Phase 4: 完成 T10(安全扫描CI) + Dependabot

---

## T6 / T11 — 阻塞任务

### T6 — CDK token 改为 Header 传递

| 字段 | 内容 |
|------|------|
| **状态** | 🚫 已阻塞 |
| **根本原因** | `check.rs:576-577` 中 CDK 通过 URL 查询参数 `?cdk=...` 传递。改为 HTTP Header 需要 MirrorChyan API 端支持 |
| **阻塞条件** | 需要确认 MirrorChyan API 是否接受 `Authorization: Bearer <cdk>` 或 `X-MirrorChyan-CDK: <cdk>` header |
| **后续操作** | 联系 MirrorChyan 团队确认 API 规范 → 修改 `check.rs:576-577` 即可 |
| **涉及文件** | `src-tauri/src/updater/check.rs:576-577` |

### T11 — 镜像源增加 checksum 返回

| 字段 | 内容 |
|------|------|
| **状态** | 🚫 已阻塞 |
| **根本原因** | MirrorChyan API 响应中不包含 asset SHA-256 checksum，导致 MirrorChyan 源下载无法做哈希完整性验证 |
| **阻塞条件** | MirrorChyan API 需在响应中增加 `sha256` 或 `checksum` 字段 |
| **后续操作** | 联系 MirrorChyan 团队 → 在 `check.rs:673` 和 `download.rs:713-716` 启用验证 |
| **涉及文件** | `src-tauri/src/updater/check.rs`, `src-tauri/src/updater/download.rs` |

---

*关联文档: `Docs/2026-06-15-花笺安全审查报告.md`, `Docs/2026-06-15-花笺修复任务计划.md`, `Docs/2026-06-15-花笺项目索引与任务追踪.md`*
